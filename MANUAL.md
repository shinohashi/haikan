# 配管拾い出し集計ツール v3.1 操作マニュアル

対象ファイル：`piping_takeoff_tool_v3.1.html`

このマニュアルは「① クイックスタート（簡易版）」と「② 詳細マニュアル（標準版）」の 2 部構成です。まず急ぐ人は ① だけ読めば一通り作業できます。

---

## 0. このツールについて

- 配管図（アイソメ図）をなぞって配管ネットワークを作り、**CSV 出力 → Excel で積算**するための「拾い出し補助ツール」です。
- HTML ファイル単体で動作します（インストール・通信不要）。
- **ノード・区間などの作業データ**はブラウザの localStorage に自動保存され、同じ PC・同じブラウザなら次回も復元されます（作業データとして保存されるのは ノード / 区間 / 採番カウンタ。縮尺設定は別キーで保存）。
- **ただし図面画像は保存されません。** ブラウザを開き直したときは、図面の読み込みからやり直してください（ノード座標は画像基準で保持されているので、同じ画像を読み込めば位置は合います）。
- 自動読図・見積計算・工程管理・モバイル UI はこのツールの範囲外です。

---

## 1. 起動方法

`piping_takeoff_tool_v3.1.html` をダブルクリックして開く（またはブラウザのウィンドウにドラッグ＆ドロップ）だけです。

---

# ① クイックスタート（簡易版・最短手順）

画面上部のステッパー **`① 図面を読込 → ② ノード配置 → ③ 区間を確定 → ④ CSV 出力`** に沿って進めます。今どのステップにいるかは色で強調され、状態バーの「次にやること」にもヒントが出ます。

1. **図面を読み込む**
   右ペイン上部の「ファイル選択」から配管図の画像ファイルを選びます。

2. **ノードを置く**
   右ペインが「ノード追加」モードのまま、図面上の継手・分岐・機器の位置をクリックしていきます。
   先に左「入力」タブの「ラベル」「種類」を入れておくと、名前付きで配置できます（空欄なら `N1, N2…` の自動名）。

3. **区間（配管の 1 本）を入力する**
   左「入力」タブの「区間（エッジ）登録」で
   `始点ノード` ／ `終点ノード` ／ `長さ(m)` ／ 管種・口径などを入れて **「区間を確定 →」** を押します。
   確定すると終点が次の始点に自動で引き継がれるので、続けて入力していけます。

4. **CSV を出力する**
   全部入れ終わったら **「出力」タブ** で必要なボタン（例：`直管集計CSV`、`歩掛連携CSV`、`全集計テーブル統合CSV` など）を押してダウンロードし、Excel で積算します。

> 区間を間違えたら：右ペインを「ノード選択」モードにして区間線をクリック → 「選択した区間を削除」。直前の 1 区間なら「↩ 元に戻す」（`Ctrl+Z`）。

---

# ② 詳細マニュアル（標準版）

## 2. 画面構成

- **左ペイン**：タブ式（`入力` / `区間一覧` / `集計結果` / `出力`）。
  上部にワークフローのステッパーと「現在状態バー」（ノード数・区間数・最新区間・次にやることのヒント）があります。
- **中央の仕切り（divider）**：左右の境目をドラッグすると、左右ペインの幅を変えられます。
- **右ペイン**：図面キャンバス。上から
  ツールバー（ファイル選択 ／ モード切替ボタン ／「ノード名」表示トグル ／ ズーム % 表示）→
  縮尺ツールバー → 図面エリア → 折りたたみ式「ノード一覧」。

---

## 3. 「入力」タブ

### 3-1. ノード登録

- **ラベル**：例「メーター」「分岐A」「給湯器」。
- **種類**：`分岐点` / `継手` / `メーター` / `機器接続` / `末端` / `バルブ設置点`。
  （`機器接続` のノードは「集計結果」タブの「接続機器一覧」に出ます）
- **「ノードを登録」ボタン**を押すと…
  - 図面を読み込み済みのとき：その後 **図面上をクリックした位置** にノードが置かれます（`ESC` で取消）。
  - 図面未読込のとき：画面中央付近に置かれます。
- 図面読込済みなら、ラベル欄に名前を入れてから **図面を直接クリックしてもOK**（「ノード追加」モード時）。空欄なら `N1, N2…` の自動名になります。
- 既に置いたノードは、後述の「ノード選択」モードでドラッグして位置を直せます。

### 3-2. 区間（エッジ）登録 — 2 ノードをつないで 1 区間

| 項目 | 説明 |
| --- | --- |
| 始点ノード / 終点ノード | プルダウンから選択。前区間の終点が「前区間から引き継ぎ」バッジ付きで自動セットされます。 |
| 長さ (m) | 手入力。**縮尺設定済みなら「自動計算」ボタン**で、選択中の始点・終点間の実寸を長さ欄に入れられます。 |
| 系統 | 既定「ガス配管」。〔保持〕＝確定後も値が残り、次の区間に引き継がれます。 |
| 管種 | SGP（白）/ SGP（黒）/ PLV-VA / PLV-VB / PLV-VD / SUS / 被覆鋼管 / フレキ管 / MF（メタルフレキ）/ PE管 / VP / HIVP / 銅管 / 耐火二層管。〔保持〕 |
| 口径 | 13A / 15A / 20A / 25A / 32A / 40A / 50A / 65A / 75A / 80A / 100A / 125A / 150A / 200A。〔保持〕 |
| 方向 | 横引き（水平）/ 立上り（下から上）/ 立下り（上から下）。〔保持〕 |
| 施工場所 | 屋内露出 / 屋内隠蔽 / 屋外露出 / 屋外埋設 / 機械室 / シャフト内 / ピット内 / コンクリート埋設。〔保持〕 |
| メモ | 任意。確定するとクリアされます。 |
| バルブ類 | プルダウン（ガス栓 / ボール弁 / ゲート弁 / チャッキ弁 / ストレーナ / 減圧弁 / 安全弁）で選んで「＋」。複数追加でき、タグの「✕」で削除。 |
| 経由ノード（任意） | 「▶ 経由ノード（任意）」を開いて、その区間の途中を通るノードを順序付きで追加（↑↓で並べ替え、✕で削除）。 |

**ボタン**

- **「区間を確定 →」**：入力内容で 1 区間を登録します。
- **「↩ 元に戻す」**：直前に確定した区間を取り消します（`Ctrl+Z` でも可）。
- **「選択した区間を削除」**：右ペインを「ノード選択」モードにして区間線をクリックしてから押します（`Delete` / `Backspace` でも可）。

---

## 4. 図面キャンバスの操作

- **モードボタン（右ペイン上部）**
  - `ノード追加`：空白をクリックでノード追加（既存ノードの上をクリックしても何も起きません）。
  - `ノード選択`：ノードをドラッグして移動／区間線をクリックして選択（削除対象になる）。
  - `パン`：ドラッグで図面を移動。
- **ズーム**：`Ctrl`（Mac は `⌘`）＋マウスホイール、またはトラックパッドのピンチ。
  **単なるホイールはスクロール（パン）** で、ズームにはなりません。現在の倍率は右上に % 表示。
- **「ノード名」トグル**：キャンバス上のラベル表示の ON / OFF。
- **ノード一覧（下部・折りたたみ）**：ヘッダをクリックで開閉。各行でラベルをクリックして編集、「✕」で削除できます。

---

## 5. 縮尺設定（長さを自動計算したいとき）

1. 右ペインの **「縮尺設定」** ボタンを押す。
2. 図面上の **既知寸法の 2 点をクリック**。
3. その 2 点間の実寸（数値）と単位（mm / m）を入力 →「設定」。
4. 設定後、「入力」タブの **「自動計算」ボタンが有効** になり、選択中の始点・終点ノード間の距離から長さを算出して長さ欄に入れてくれます。

- 「縮尺リセット」で解除します。
- 設定の途中で `ESC` を押すとキャンセルできます。

---

## 6. 「区間一覧」タブ

確定した区間が一覧表示されます。各行から個別に「削除」できます。
（区間が 1 件もないときは、その旨と入力タブへの案内が表示されます）

---

## 7. 「集計結果」タブ

- 上部の設定
  - **所要係数（ロス率）**：既定 `1.05`
  - **定尺長さ (m)**：既定 `4`
  - どちらも変更すると自動で再計算されます。
- 表示される 4 つのテーブル
  1. **直管集計**：系統 / 管種 / 口径 / 施工場所 / 合計(m) / 定尺本数 / 所要数量
  2. **継手集計（自動推定）**：系統 / 管種 / 口径 / 種類 / 数量
  3. **バルブ類集計**：系統 / 種類 / 口径 / 数量
  4. **接続機器一覧**：種類が「機器接続」のノード（ノードID / 機器名 / 接続管種 / 接続口径 / 系統）

---

## 8. 「出力」タブ（CSV / JSON）

| ボタン | 内容 |
| --- | --- |
| ネットワークJSON出力（GAS連携用） | version / timestamp / ノード / 区間 を含む JSON。**GAS連携・外部保管用**です。**このツール単体で JSON を読み込み直す機能はありません**（取り込みボタンはありません）。 |
| 直管集計CSV | 直管集計テーブルを CSV 出力。 |
| 継手集計CSV | 継手集計テーブルを CSV 出力。 |
| バルブ類CSV | バルブ類集計テーブルを CSV 出力。 |
| 全集計テーブル統合CSV | 上記の集計テーブルをまとめて 1 ファイルに。 |
| エッジ生データCSV | 区間の生データを CSV 出力。 |
| 歩掛連携CSV | 歩掛（Excel 側）連携用の CSV。 |

> 用途の目安：**積算用は CSV**、**外部連携・記録用は JSON**。

### データ管理

- **「全データクリア」**：ノード・区間データ（localStorage の `pipingToolData`）をすべて消去します。**元に戻せません**。
  （縮尺設定 `pipingScale` はこの操作では消えません。「保存データをすべて消す」操作ではありません）

---

## 9. データ保存について

- ノード・区間を編集するたびに、ブラウザの localStorage に自動保存されます（保存対象：ノード / 区間 / 採番カウンタ）。縮尺設定も別キーで保存されます。
  → 同じ PC・同じブラウザなら、次に開いたとき作業データは続きから使えます。
- **図面画像は保存されません。** ブラウザを開き直したら、図面の読み込みからやり直してください。ノード座標は画像基準なので、同じ画像を読み込めば位置は合います。
- 外部に渡す／別 PC で参照する場合は「ネットワークJSON出力」でファイル保存します。ただし**ツールに JSON を読み込み直す機能はない**ため、ツール内での「復旧」目的には使えません（連携・記録用と考えてください）。

---

## 10. キーボードショートカット

| キー | 動作 |
| --- | --- |
| `Ctrl+Z` | 直前に確定した区間を元に戻す（実装上 `Ctrl` のみ。Mac の `⌘+Z` は効かないので、画面の「↩ 元に戻す」ボタンを使う） |
| `Delete` / `Backspace` | 選択中の区間を削除（入力欄にフォーカスしていないとき） |
| `ESC` | 縮尺設定の途中／ノード配置の待機をキャンセル |

---

## 11. 困ったとき（簡易トラブルシュート）

| 症状 | 確認すること |
| --- | --- |
| ノードが置けない | 図面（画像）を読み込んでいるか／「ノード追加」モードになっているか。 |
| 区間が確定できない | 始点・終点が選択されているか、長さ(m) が 0 より大きいか（始点＝終点は不可）。 |
| 「自動計算」ボタンが押せない | 縮尺が未設定です。先に「縮尺設定」を行ってください。 |
| 表示がおかしい | ブラウザを再読込（ノード・区間データは localStorage に残ります。図面画像だけ読み直してください）。心配なら先に「ネットワークJSON出力」「各種CSV出力」で内容を書き出してから操作してください（JSON はツールに戻せないので、あくまで記録目的）。 |
