# 配管拾い出しツール（haikan）設計ルール

## 1. 基本思想

- 見た目より「業務が迷わず進むこと」を最優先
- UIは道具。装飾ではなく作業効率のために存在する
- 情報は「今必要なもの」から順に見せる
- 色ではなく、配置・余白・階層で整理する
- 数字・長さ・数量は読みやすさを最優先にする

---

## 2. スコープ（今回の範囲）

- HTML単体で動作
- 図面をなぞって配管ネットワークを作る
- CSV出力まで
- 歩掛・見積はExcel側で処理

※ GAS化や工程管理は将来検討（現時点では決め打ちしない）

---

## 3. UI設計の共通原則

### 3.1 情報構造優先

- UIの違和感はまず「構造（DOM）」を疑う
- marginやgapで調整する前に、配置を見直す
- 1行で破綻するUIは構造ミスの可能性が高い

### 3.2 表示優先順位

1. 対象（図面・区間）
2. 操作（登録・確定）
3. 数値（長さ・数量）
4. 補助情報

---

### 3.3 シンプルUI

- 強いボタンは1画面1つ
- 色で区別しすぎない
- 余白で整理する
- テーブル・リスト中心

---

## 4. 表示トラブル防止ルール（最重要）

### 4.1 表示されない時の切り分け

以下の順で確認すること：

1. DOMに存在するか
2. CSSで非表示になっていないか
3. render処理が走っているか
4. データがあるか

原則：
👉 「見えない問題の8割はCSS」

---

### 4.2 DOM設計

- 共通UI（modal / overlay）は親の影響を受けない位置に置く
- display:none の親の中に重要UIを置かない
- position:fixed でも親のdisplayに影響される

---

### 4.3 表示とデータの分離

- 「表示されない」と「データがない」を混同しない
- 表示用データと計算用データを分離する

NG例：
- 計算補正で元データを上書きする

---

### 4.4 silent fail禁止

- 値が取得できない場合は空欄にしない
- `—` などの明示表示を行う
- 開発時はconsoleログを残す

---

## 5. データ構造ルール

- nodes / edges 構造は壊さない
- 同じ意味のキーは統一する
- key不一致による表示欠落を防ぐ

例：
- id / zone_id / _id などを混在させない

---

## 6. 既存互換優先ルール

- 既存の構造を壊さない
- CSV列名を変更しない
- 小さく修正する（最小変更）

---

## 7. UI変更ルール

### NG

- inline styleで場当たり対応
- margin-left:autoで位置調整
- widthで無理やり整える

### OK

- classで共通化
- wrapper構造で整理
- 行単位で再設計

---

## 8. モバイル・タッチ前提ルール

- タップ領域は大きく（44px以上）
- 1画面1目的
- 内部スクロールは避ける
- bodyスクロールを基本とする

---

## 9. 数値・単位ルール

- 数値は右寄せ
- 単位は必ず表示
- 桁区切りを行う

例：
- 1,250 mm
- 8.5 m

---

## 10. 図面UIルール（配管版）

- 図面を主役にする
- オーバーレイは薄くする
- 必要な対象だけ強調する
- 全面デバッグ表示は禁止

---

## 11. 操作導線ルール

- 入力 → 確認 → 確定 の流れを明確にする
- 操作後は必ずフィードバックを出す
- 「押したか分からない」を作らない

---

## 12. AI修正ルール

AIに修正させる場合：

- 既存構造（nodes / edges）を壊さない
- 新しいIDや関数をむやみに追加しない
- class追加と構造整理で対応する
- UI変更時は確認手順もセットで提示する

---

## 13. 原則まとめ

- UIは「構造」で作る
- 表示不具合はCSSとDOMから疑う
- データと表示を分ける
- 最小修正で進める
- 今回は拾い出し＋CSVまでで十分