# CLAUDE.md — haikan（配管拾い出しツール）

---

## 0. 目的

配管図（アイソメ図）から配管ネットワークを半自動で拾い出し、
CSV出力してExcelで積算できる状態を作る。

本ツールは「拾い出し補助ツール」であり、
見積・工程・モバイルはスコープ外とする。

---

## 1. 最重要原則（絶対遵守）

### 1.1 最小変更原則
- 既存コードを壊さない
- 大規模リファクタ禁止
- 追加ベースで修正する

---

### 1.2 データ構造維持

以下は変更禁止：

- nodes（ノード）
- edges（区間）

👉 配管ネットワークの中核構造

---

### 1.3 スコープ厳守

今回の範囲：

- 図面上でノード配置
- 区間入力（長さ・管種・口径）
- CSV出力

やらないこと：

- 自動読図
- 見積計算
- 工程管理
- モバイルUI
- GAS化（将来は検討可）

---

## 2. 設計思想（共通抽出）

### 2.1 UIは構造で作る

- 見た目よりDOM構造を優先
- margin / gapで無理に調整しない
- wrapper構造で整える

👉 UI違和感は構造ミス

---

### 2.2 データと表示を分離

- 表示用と計算用を混ぜない
- 表示不具合とデータ欠損を分ける

禁止：
- 計算結果で元データを書き換える

---

### 2.3 サーバー/データが正（原則抽象化）

- UIの値を信用しない
- 状態はデータから再構築する

（inventoryの思想を軽量適用）

---

### 2.4 append思想

- 上書きより追加
- 既存データを壊さない

---

## 3. 表示トラブル防止（最重要）

### 3.1 表示されない時のチェック順

1. DOMに存在するか
2. CSSで非表示か
3. render処理が走っているか
4. データがあるか

👉 8割はCSS or DOM

---

### 3.2 DOMルール

- display:none の親に重要UIを置かない
- modal / overlay は外に置く
- position:fixedでも親の影響を受ける

---

### 3.3 silent fail禁止

- 値なしは空白にしない
- `—` を表示
- consoleログを残す

---

## 4. UI設計ルール

### 4.1 原則

- 1画面1目的
- 入力は最小限
- 数値は右寄せ
- 単位は必ず表示

---

### 4.2 操作導線

入力 → 確認 → 確定

- 押した結果は必ず見えるようにする
- フィードバックを返す

---

### 4.3 タッチ前提

- タップ領域を広く
- 内部スクロール禁止
- bodyスクロールを使う

---

## 5. データルール

### 5.1 キー整合性

- 同じ意味のキーは統一
- id / _id / nodeId の混在禁止

---

### 5.2 変更禁止

- CSV列名
- nodes / edges構造

---

## 6. 更新モデル（inventory由来）

- 更新後は再描画
- UIは状態を持たない
- 再取得ベースで描画

---

## 7. 実装ルール

### 7.1 禁止

- inline style乱用
- margin-left:auto調整
- widthで無理やり揃える

---

### 7.2 推奨

- classで整理
- wrapper追加
- 行単位で再設計

---

## 8. AI修正ルール

AIは必ず守ること：

- nodes / edgesを壊さない
- 既存関数を削除しない
- 小さく修正する
- UI変更時は確認手順を提示する

---

## 9. 確認ルール（超重要）

完了報告前に必ず：

- 実際にブラウザで表示確認
- 入力→確定→再表示の流れ確認
- consoleだけで判断しない

禁止：
- 「修正済み」と先に言うこと

---

## 10. 原則まとめ

- UIは構造で作る
- 表示不具合はCSS/DOMを疑う
- データと表示を分離
- 最小変更で進める
- 今回は拾い＋CSVで十分